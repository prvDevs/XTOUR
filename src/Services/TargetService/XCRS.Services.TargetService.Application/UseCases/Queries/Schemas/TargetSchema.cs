using Microsoft.Extensions.DependencyInjection;

namespace XCRS.Services.TargetService.Application.UseCases.Queries.Schemas
{
    //public class TargetSchema : Schema
    //{
    //    public TargetSchema(IServiceProvider provider) : base(provider)
    //    {
    //        Query = provider.GetRequiredService<TargetQuery>() ?? throw new ArgumentNullException(nameof(provider));
    //        FieldMiddleware.Use(new InstrumentFieldsMiddleware());
    //    }
    //}
}
